import React from 'react';
import Header from '../../components/Header/Header';

import { MainContainer } from './styles';

const Main = (props) => {
  const { } = props;

  return (
    <MainContainer>
      <Header />
    </MainContainer>
  );
};

export default Main;